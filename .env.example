# ─── Backend API ────────────────────────────────────────────────────────────
# The Express backend (see backend/) — everything else (DB, auth secret,
# Gemini, Gmail, SSLCOMMERZ, cron secret) now lives in backend/.env instead.
NEXT_PUBLIC_API_URL="http://localhost:4000"
